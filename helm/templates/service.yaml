apiVersion: v1
kind: Service
metadata:
  name: {{ include "shank-site.fullname" . }}
  labels:
    {{- include "shank-site.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "shank-site.selectorLabels" . | nindent 4 }}
