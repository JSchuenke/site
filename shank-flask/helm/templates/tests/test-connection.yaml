apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "shank-flask.fullname" . }}-test-connection"
  labels:
    {{- include "shank-flask.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "shank-flask.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
