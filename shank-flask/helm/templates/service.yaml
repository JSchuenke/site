apiVersion: v1
kind: Service
metadata:
  name: {{ include "shank-flask.fullname" . }}
  labels:
    {{- include "shank-flask.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "shank-flask.selectorLabels" . | nindent 4 }}
